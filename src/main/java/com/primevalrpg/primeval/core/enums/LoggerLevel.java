package com.primevalrpg.primeval.core.enums;

/**
 * Logger Enum used for setting logging levels and colours for ease of reliability
 */
public enum LoggerLevel {
    INFO, STARTUP, SHUTDOWN, ERROR, DEBUG
}
